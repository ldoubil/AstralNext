// lib/ui/pages/nodes_page.dart
import 'package:flutter/material.dart';

class NodesPage extends StatelessWidget {
  const NodesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        '节点列表 / 状态',
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
