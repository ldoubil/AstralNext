name: vpn_service_plugin
description: Android VPN service plugin for Astral
version: 0.0.1

environment:
  sdk: ^3.8.0
  flutter: '>=3.41.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.astral.vpn_service
        pluginClass: VpnServicePlugin
